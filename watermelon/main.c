#include <bits/stdc++.h>
using namespace std;

int main() {
  int w;
  cin >> w;
  // printf("%s\n", w % 2 == 0 && w > 2 ? "YES" : "NO") is the same
  cout << (w % 2 == 0 && w > 2 ? "YES" : "NO") << "\n";

  return 0;
}


